<p>{{ expense.name }}</p>
<p>{{ dateFormate expense.date }}</p>
<p>{{ expense.amount }}</p>
<p>{{ expense.category }}</p>
<a href="/expenses/{{ expense._id }}/edit">edit</a>
<a href="/">back</a>
<form action="/expenses/{{ expense._id }}/delete" method="POST" style="display: inline;">
  <button type="submit">delete</button>
</form>

